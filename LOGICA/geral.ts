/*
1. Defina o que é uma variável na programação.
2. Escreva com as suas palavras a diferença entre as declarações const, let e var e como elas podem ser aplicadas na prática.
3. Explique a diferença entre as variáveis do tipo objeto e lista.
*/

/*

1. O que é uma variável?

Uma variável é um espaço na memória do computador 

2. Diferenças entre as declarações const, let e var:

const:
Uma constante é uma variável cujo valor não pode ser alterado após a sua inicialização.

let:
Uma variável local de escopo, cujo valor pode ser alterado dentro do 

var:
Uma variável global, cujo valor pode ser acessado e alterado em qualquer lugar do programa.

3. Diferenças entre objetos e listas:

Objeto:
É uma coleção de pares chave-valor, onde cada chave é um nome único para um valor associado.

Estrutura: {"chave1": valor1, "chave2": valor2, ...}.
Acessando valores: Utilize a notação de ponto ou colchetes: objeto.chave1 ou objeto["chave2"].

Lista:
É uma coleção ordenada de valores, onde cada valor tem um índice.
Estrutura: [valor1, valor2, ..., valorN].
Acessando valores: Utilize colchetes: lista[indice].

*/